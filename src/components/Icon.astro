---
export type Props = {
  icon: IconName;
  class?: string;
};

import { Icon as AstroIcon } from "astro-icon/components";
import { icons, type IconName } from "../icons";

const { icon, class: className } = Astro.props;

const iconName = icons[icon];
---

<AstroIcon class={className} name={iconName} />
